{{- if and .Values.networkPolicy.enabled .Values.migrate.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "soniq.migrateName" . }}
  namespace: {{ include "soniq.namespace" . }}
  labels:
    app.kubernetes.io/name: {{ include "soniq.migrateName" . }}
    app.kubernetes.io/component: migrate
    {{- include "soniq.commonLabels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: {{ include "soniq.migrateName" . }}
      app.kubernetes.io/component: migrate
  policyTypes:
    - Ingress
    - Egress
  ingress: []
  egress:
    {{- include "soniq.networkPolicyEgress" . | nindent 4 }}
    {{- with .Values.networkPolicy.migrate.extraEgress }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
{{- end }}
