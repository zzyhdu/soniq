{{- if .Values.api.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "soniq.apiName" . }}
  namespace: {{ include "soniq.namespace" . }}
  labels:
    app.kubernetes.io/name: {{ include "soniq.apiName" . }}
    app.kubernetes.io/component: api
    {{- include "soniq.commonLabels" . | nindent 4 }}
spec:
  {{- if ne .Values.api.pdb.minAvailable nil }}
  minAvailable: {{ .Values.api.pdb.minAvailable }}
  {{- end }}
  {{- if ne .Values.api.pdb.maxUnavailable nil }}
  maxUnavailable: {{ .Values.api.pdb.maxUnavailable }}
  {{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "soniq.apiName" . }}
      app.kubernetes.io/component: api
{{- end }}
